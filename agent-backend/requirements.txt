# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # multipart/form-data support (PDF upload endpoint)

# ── LLM providers ──────────────────────────────────────────────────────────────
groq>=1.0.0                   # Groq SDK — fallback provider (0.9.x breaks on httpx 0.28+)
google-genai>=2.0.0           # Gemini free tier: 1M TPM, no rate limits (new official SDK)
openai>=1.30.0                # OpenAI-compat SDK — used for NVIDIA NIM and Ollama

# ── Vector store & embeddings ──────────────────────────────────────────────────
chromadb==0.5.3
sentence-transformers==2.7.0  # local embedding model; swap for OpenAI embeds if preferred

# ── PDF ingestion ──────────────────────────────────────────────────────────────
pypdf==4.2.0

# ── Data validation & config ───────────────────────────────────────────────────
pydantic>=2.7.1
pydantic-settings>=2.3.0

# ── HTTP (used in tests / health checks) ───────────────────────────────────────
httpx>=0.27.0

# ── LLM JSON resilience ────────────────────────────────────────────────────────
# Repairs malformed JSON from LLM outputs (trailing commas, comments, prose
# bleed-through, single-quoted keys). Used as fallback when json.loads fails.
json-repair>=0.30.0

# ── Headless CAD validation ────────────────────────────────────────────────────
# OCCT-backed parametric CAD. Used by validation/ to run OperationGraphs
# headlessly in CI without needing SolidWorks. Apache-2.0.
build123d>=0.7.0
# chromadb 0.5.3 still requires numpy<2 (np.float_ removed in 2.0). build123d
# pulls numpy 2.x; pin it back so both coexist.
numpy<2.0,>=1.26

# ── Testing ─────────────────────────────────────────────────────────────────────
pytest>=9.0.0
